---
# tasks file for desktop
- name: install i3 window manager
  apt:
    name: ['i3-wm', 'xinit']

- name: install i3 status-bar and menu
  apt:
    name: ['i3status', 'rofi']

- name: set i3 autostart in /etc/profile
  lineinfile:
    path: /etc/profile
    line: startx /usr/bin/i3
